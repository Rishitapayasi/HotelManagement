
<%= render partial: 'hotels', locals: {hotels: @hotels}  %>

<div>

  <%= link_to "Back to hotels", hotels_path %> 

  <%if current_user.type == 'Owner'%>
    <%= link_to "Edit this hotel", edit_hotel_path(@hotels) %> 
    <%= button_to "Destroy this hotel", @hotels, method: :delete %>
    <%= link_to "Add room to hotel",new_room_path %>
    <%end%>
</div> 